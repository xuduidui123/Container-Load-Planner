# 自动排柜工具 · 使用说明

## 第一次使用
双击 `自动排柜工具.html`，用 Chrome / Edge 打开。免安装、可离线、数据不上传。
想先看效果：展开「或粘贴」里点「载入示例数据 → 开始排柜」。

## 日常使用（三步）
1. **上传数据**：把客户 PO 明细的 Excel（.xlsx）拖到虚线框，或点框选择文件。也支持 CSV，或展开下面「或粘贴」直接从 Excel 复制粘贴。
   - 读第一个工作表、第一行当表头。上传后核对「列对应」（工具会自动猜）。
   - 必填列：PO号、体积、交期/船期；建议一起带上：客户、重量、个数、箱数。
2. **排柜设置**：
   - **分仓依据**：勾「不同客户分开排柜」；船期默认「按具体船期（同一天才同柜，同月不同船期也不拼柜）」，也可改「按月份」或「不分」。勾选的项不一样就不会装同一个柜。柜名会带上这些信息，如 `客户A-2026-08-31-40HQ-1`。
   - **柜型容量**：设各柜型的默认体积、期望体积（不想装太满就填小一点，如 40HQ 填 56）、最大载重、中文名。「默认可用」是新客户的默认勾选状态。
   - **各客户可用柜型**：上传带客户列的表后会自动列出每个客户，可单独勾选各自能用的柜型（如某客户不接受 20 尺柜就取消其 20小柜）。这样接受 20 尺柜的客户仍能排 20小柜，互不影响。
   - **重量列口径**：你们表里的「外箱-毛重kg」是每箱重量，选「每箱毛重」（总重=每箱×箱数）；若某表填的是整批总重就选「整批总重」。
   - 需要按外箱拆分 PO 才勾「允许拆分」，否则整个 PO 不拆。
   - 「最低成柜体积利用率」默认 85%，达不到的剩余货算散货。
3. **开始排柜** → 看结果：每个柜含哪些 PO、利用率、重量；原表加了「排柜方案」列。
   - 「下载 Excel」得到一个多 Sheet 工作簿：第一个 Sheet「总表」按客户汇总（客户 / 柜型或散货 / 柜数 / 涵盖PO数），后面每个客户各一个 Sheet（原表全列 + 排柜方案列）。也可「下载 CSV」或「复制结果表」粘回原 Excel（这两种是单表）。

## 排柜规则
- 先按「分仓依据」（客户 / 船期）分组，同组的货才互相拼柜；柜名带客户和月份。
- 以**体积**凑柜，**重量**为不能超的红线（超最大载重不允许）。
- 多柜型可用时优先用大柜减少柜数；最后没装满的柜自动挑最贴合的小柜型。
- 达不到成柜利用率（且重量也没装满）的剩余货 → 标「散货」。

## 出问题怎么办
- **列对不上**：在「列对应」下拉里手动选正确的列。
- **月份显示「未知月」**：把交期列改成 `2026-08-31` 这种日期格式再传。
- **散货偏多**：把船期从「按具体船期」改成「按月份」，同月的货就能拼到一起；或调低成柜利用率。
- **复制没反应**：改用「下载 Excel / CSV」。
- **某 PO 提示"超柜需拆分"**：该 PO 单独就超一个柜，勾「允许拆分」或人工处理。

## 想改功能
把本文件夹的 `需求文档.md` 连同工具一起发给 AI，说要改什么即可。
