# 自动排柜工具 — 需求文档

## 一句话目标
仓库每月出货前，把一批 PO 明细导入工具，自动算出：这些货能凑成几个整柜、哪些 PO 拼哪个柜、剩下哪些走散货，并在原表格最后加一列写好结果（如「6月40HQ-1」「散货」）供下载。

## 使用场景
- 使用者：仓库（业务员基本不用）。
- 频率：每月出货高峰，一批几十上百个 PO。
- 设备：电脑浏览器，双击打开单个 HTML 文件即可，免安装、可离线。

## 输入
从 Excel 整块粘贴（或上传 CSV）一张表，每行一个 PO，包含以下列（列名可在工具里对应，不必完全一致）：

| 列 | 说明 |
|---|---|
| PO号 | 订单号 |
| 品名 | 产品名称 |
| 体积 | 这批货的总体积（CBM，立方米） |
| 重量 | 这批货的总毛重（kg） |
| 个数 | 这批货的数量 |
| 外箱数 | 这批货的外箱总数（拆分 PO 时用来按箱拆） |
| 交期 | 交期 / EHD / ship date，用来取月份命名柜子 |

## 输出
- 在原表格最右加一列「排柜方案」，值为 `6月40HQ-1`、`6月40GP-2` 或 `散货`。
- 允许拆分且某 PO 被拆时，写成 `6月40HQ-1(30箱) / 6月40HQ-2(20箱)`。
- 结果可「复制回 Excel」和「下载 CSV（可用 Excel 打开）」。
- 上方给一份汇总：每个柜含哪些 PO、体积利用率、总重量；散货合计体积/重量。

## 排柜规则（已与 Sherry 确认）
1. **排柜依据**：以**体积为主**，**重量为不能超的红线**（体积凑柜，但总重不得超过柜子最大载重）。
2. **按交期月份分组**：先按交期的月份把 PO 分组，同月份的货才互相拼柜；柜子以该月份命名。
3. **柜型可勾选**：20GP / 40GP / 40HQ 可各自勾选是否可用（有的客户不接受 20 尺柜）。
4. **每个柜型两个体积**：
   - `默认体积`（柜子容量参考值，可编辑）；
   - `期望体积`（可选，右侧填写）——不填用默认，填了就按填的算（用于「我不想装那么满」）。
   - 排柜时以**期望体积作为装载上限**，不超过它。
5. **载重红线**：每个柜型有`最大载重(kg)`，装柜总重不得超过。
6. **是否允许拆分 PO**：做成一个勾选项。
   - 不勾选（默认）：整个 PO 不拆，必须整体进一个柜。
   - 勾选：一个 PO 的外箱可以拆到多个柜（按 体积/外箱数 得到单箱体积，按 重量/外箱数 得到单箱重量分配）。
   - 单个 PO 本身体积就超过一个柜时：不拆模式下会提示该 PO 需单独处理。
7. **整柜 vs 散货门槛**：设一个`最低成柜体积利用率`（默认 85%，可改）。凑出的一个柜若体积利用率≥门槛→算整柜；达不到门槛的剩余货→标为「散货」。

## 我的假设（请逐条核对，不对就告诉我）
- A. 表格里「体积」「重量」是**这批货的合计值**（不是单件/单箱值）。单箱体积=体积÷外箱数。
- B. **同一个柜只放同月份的货**；不同交期月份不混柜。
- C. 多个柜型都勾选时，优先用能减少柜数的**较大柜型**装；最后一个没装满的柜，会自动选「装得下且最贴合」的较小柜型来提高利用率。
- D. 柜子编号按该月该柜型顺序排：`6月40HQ-1`、`6月40HQ-2`… 不同柜型分别编号。
- E. 柜型容量默认值（可在工具里改）：20GP 体积28 / 载重28000kg；40GP 体积58 / 载重26500kg；40HQ 体积68 / 载重26500kg。
- F. 输出用 CSV + 复制两种方式（零依赖、Excel 能直接打开），暂不生成 .xlsx。

## 方案形态
与现有「箱规计算器」一致：**纯前端、零依赖、单文件 HTML**，放在 `自动排柜工具/` 文件夹，双击浏览器打开即用，可部署到 GitHub Pages 供团队共用。数据只在本地浏览器，不上传。

## V2 更新（2026-07-25，据真实客户PO文件）
- **改为上传 Excel（.xlsx）**：不再需要粘贴，拖拽/选择文件即可；仍保留粘贴和 CSV 作为备用。已把 Excel 读写能力（SheetJS）内嵌进单文件，保持离线可用。
- **分仓依据可自定义**：不同**客户**必须分开订舱、不同客户的货不能同柜 → 加「不同客户分开排柜」勾选（默认开）；**船期**可选「按月份 / 按具体船期 / 不分」。柜名带上分仓信息，如 `Tesco-8月40HQ-1`。
- **重量口径修正**：真实文件「外箱-毛重kg」是**每箱毛重**（非整批总重）。加「每箱毛重 / 整批总重」口径选择，默认每箱（总重=每箱×箱数）。
- **导出**：新增真正的 `.xlsx` 导出（原表全列 + 排柜方案列），CSV/复制保留。
- 列名自动识别已适配该文件表头：客户 / 船期(ship_date) / PO号码 / 中文品名 / 个数 / 箱数 / 外箱-毛重kg / 总体积(m³)。

## V3 更新（2026-07-25）
- **默认按具体船期订舱**：同月不同船期不能拼同一个柜（船期分仓默认「按具体船期」）。
- **导入一张所有客户的总表**，导出为**多 Sheet Excel**：
  - Sheet「总表」：每客户按柜型汇总，列＝客户名称 / 柜型或散货 / 柜子数量（散货显示散货PO数）/ 涵盖PO数量。
  - 其后每个客户各一个 Sheet（Tesco、B客户、C客户…），含该客户原表全列 + 排柜方案列，分开查看。
  - Sheet 名做了 Excel 合规处理（去非法字符、限长、去重）。

## 待验证（原型后确认）
- 门槛 85% 是否合适、散货判定是否符合你们实际做法。
- 拆分 PO 的输出写法是否够清楚。
- 是否需要导出成真正的 .xlsx 而不是 CSV。
