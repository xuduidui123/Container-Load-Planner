# 上传到 GitHub 并开启在线访问

下面两种方式任选其一。第一次做建议用「网页上传」，最简单。

## 方式一：网页上传（不用装任何软件）

1. 登录 [github.com](https://github.com)，右上角 **+ → New repository**。
2. 仓库名填 `container-loading-planner`（或你喜欢的名字），选 **Public**（公开＝开源），**不要**勾选 "Add a README"，点 **Create repository**。
3. 在新仓库页点 **uploading an existing file**，把本文件夹里的这几个文件拖进去上传：
   - `index.html`
   - `README.md`
   - `LICENSE`
   - `.gitignore`
   - `使用说明.md`
   - `部署说明.md`
   > ⚠️ 千万**不要**上传任何真实客户的 Excel（`.xlsx`/`.csv`）。
4. 下方点 **Commit changes** 完成上传。
5. 开启在线访问：仓库 **Settings → Pages → Source** 选 `main` 分支、目录 `/ (root)`，**Save**。
6. 等 1–2 分钟，页面会显示网址 `https://你的用户名.github.io/container-loading-planner/`，打开就是工具本身，发给同事即可。
7. 回到 `README.md`，把里面的 `USERNAME` 改成你的 GitHub 用户名（可选）。

## 方式二：命令行（已装 git）

在本文件夹里依次执行：

```bash
git init
git add .
git commit -m "初始版本：自动排柜工具"
git branch -M main
git remote add origin https://github.com/你的用户名/container-loading-planner.git
git push -u origin main
```

推送后同样到 **Settings → Pages** 开启 GitHub Pages。

## 以后要更新工具

- 网页方式：进仓库点开 `index.html` → 右上角铅笔图标编辑，或删除后重新上传新文件 → Commit。
- 命令行方式：改完文件后 `git add . && git commit -m "更新" && git push`。

GitHub Pages 会在 1–2 分钟内自动更新线上版本。
